backend:
  name: github
  repo: "BRConsulting/BR-Consulting" # Substitua com seu usuário e o nome do repositório no GitHub
  branch: main # Ou "master", dependendo do seu branch

media_folder: "static/img" # Onde as imagens serão armazenadas
public_folder: "/img"

collections:
  - name: "blog"
    label: "Blog"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Body", name: "body", widget: "markdown" }
